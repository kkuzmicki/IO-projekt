<Window x:Class="IO_projekt.AuthorListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IO_projekt"
        mc:Ignorable="d"
        Title="Autorzy" Height="500" Width="300" WindowStyle="ToolWindow">
    <Window.Resources>
        <DataTemplate x:Key="AuthorListBox">
            <DockPanel Margin="3">
                <TextBlock Text="{Binding IMIE}"/>
                <TextBlock Text=" "/>
                <TextBlock Text="{Binding NAZWISKO}"/>
            </DockPanel>
        </DataTemplate>
    </Window.Resources>

    <ListBox Margin="10,15" Name="Author" ItemsSource="{Binding Tables[0]}" ItemTemplate="{StaticResource AuthorListBox}" />

</Window>
